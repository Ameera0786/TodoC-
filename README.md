This project uses CMake. Clion automatically handles everything.
Requirements to run the program are below.
- C++ Compiler
- Cmake 4.0+

To build and run the program, clone the repository.
Configure the project with CMake and then build.
This will create TodoList which can be executed and ran. 